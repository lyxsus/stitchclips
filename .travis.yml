language: go
dist: trusty
sudo: required

go:
  - 1.8

before_install:
  - sudo add-apt-repository ppa:mc3man/trusty-media -qqy
  - sudo apt-get update -qq
  - sudo apt-get install -y ffmpeg

install:
  - go get gopkg.in/resty.v0

script: GOENV=test go test ./...